# The authoritative source for Netlify headers is public/_headers,
# which Astro copies to dist/_headers at build time.

[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.3.9"
